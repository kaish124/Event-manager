package com.reza.events.enums;

/**
 * Enum representing the status of a venue.
 */
public enum VenueStatus {
    ACTIVE,      // Venue is active and available for events
    INACTIVE,    // Venue is temporarily inactive
    MAINTENANCE, // Venue is under maintenance
    CLOSED       // Venue is permanently closed
}